context("relabel")

test_that("Relabels category items", {

})

test_that("Relabels category options", {

})

test_that("Relabels wave labels", {

})

test_that("Fails if unequal length of category items", {

})

test_that("Fails if unequal length of category options", {

})

test_that("Fails if unequal length of wave labels", {

})

test_that("Shows warning about sorting", {
  # Should show a warning telling people to sort AFTER they apply relabelling
})
